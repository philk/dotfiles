
# Program Setup
export EDITOR="vim"
export PAGER="less"

# TMUX
if [[ -n ${+commands[tmux]} ]]; then
  if [[ -z ${TMUX} ]]; then
    tmux attach-session || tmux
  fi
else
  echo 'No tmux! How Awful!'
fi

